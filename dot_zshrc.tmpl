HISTFILE=~/.zsh_history
HISTSIZE=100000
SAVEHIST=100000

setopt HIST_IGNORE_SPACE  # Don't save when prefixed with space
setopt HIST_IGNORE_DUPS   # Don't save duplicate lines
setopt SHARE_HISTORY      # Share history between sessions





# zsh pure promt
fpath+=($HOME/.zsh/pure)
autoload -U promptinit; promptinit
prompt pure

# Completion definitions must be added to fpath before compinit.
if command -v brew >/dev/null 2>&1; then
  _brew_prefix="$(brew --prefix)"
  fpath=("$_brew_prefix/share/zsh/site-functions" $fpath)
  if [[ -d "$_brew_prefix/share/zsh-completions" ]]; then
    fpath=("$_brew_prefix/share/zsh-completions" $fpath)
  fi
  unset _brew_prefix
fi

if [[ -d "$HOME/zsh-completions/src" ]]; then
  fpath=("$HOME/zsh-completions/src" $fpath)
fi

autoload -Uz compinit
compinit

# Git aliases
alias gst='git status'
alias gf='git fetch'
alias gcan!='git commit -v -a --no-edit --amend'
alias gpl='git pull'
alias gaa='git add .'
alias gcm='git commit -m'
alias gco='git checkout'
alias gcb='git checkout -b'
alias gbr='git branch'
alias gsw='git switch'
alias gswc='git switch -c'
# git stash push and promp
alias gsp='git stash push -u -m "$(date +%Y-%m-%d_%H-%M-%S)"'
alias gsa='git stash apply'
alias gsl='git stash list'
alias gsd='git stash drop'
alias gpop='git stash pop'


# finds all files recursively and sorts by last modification, ignore hidden files
alias lastmod='find . -type f -not -path "*/\.*" -exec ls -lrt {} +'

{{ if eq .chezmoi.os "linux" }}
# yay
alias yu="yay -Syu"
{{ end }}

# docker
alias fe-up="docker compose -f docker-compose-dev.yml up"
alias vimbegood="docker run -it brandoncc/vim-be-good:latest"


#Neovim
alias v="nvim ."
alias sv="sudo nvim "
# alias v='NVIM_APPNAME="lazy-nvim" nvim .'

#Kubectl
alias k="kubectl"


#Tmux 
set -o vi

export VISUAL='nvim'
export EDITOR='nvim'
[[ -n "$TMUX" ]] && export TERM="tmux-256color"

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
# export EDITOR=/usr/bin/nvim
# export VISUAL=/usr/bin/nvim

#Experimental, working on mutli system solution
# export GOROOT=/usr/local/go
# export GOPATH=$HOME/go
# export PATH=$GOPATH/bin:$GOROOT/bin:$PATH

# Custom scripts directory
export PATH=$HOME/.local/scripts:$PATH

# OS-specific configurations
{{ if eq .chezmoi.os "darwin" }}
# Android Studio (macOS)
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
export JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-17.jdk/Contents/Home
{{ end }}

# Go configuration - dynamic solution for cross-platform compatibility
if command -v go >/dev/null 2>&1; then
  export GOPATH=$(go env GOPATH 2>/dev/null || echo "$HOME/go")
  export GOROOT=$(go env GOROOT 2>/dev/null)
  
  [ -d "$GOPATH/bin" ] && export PATH="$GOPATH/bin:$PATH"
  [ -d "$GOROOT/bin" ] && export PATH="$GOROOT/bin:$PATH"
else
  DEFAULT_GOPATH="$HOME/go"
  if [ -d "$DEFAULT_GOPATH" ]; then
    export GOPATH="$DEFAULT_GOPATH"
    [ -d "$GOPATH/bin" ] && export PATH="$GOPATH/bin:$PATH"
    
    for possible_goroot in /usr/local/go /usr/lib/go /opt/go; do
      if [ -d "$possible_goroot" ] && [ -x "$possible_goroot/bin/go" ]; then
        export GOROOT="$possible_goroot"
        export PATH="$GOROOT/bin:$PATH"
        break
      fi
    done
  fi
fi
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
# Enable vi mode
bindkey -v

# bindkey -s '^f' 'nvim $(fzf)^M'
fzf-cd-widget() {
  local selected
  selected="$("$HOME/.local/scripts/fzf-dir")" || return
  [[ -n "$selected" ]] && cd -- "$selected"
  zle reset-prompt
}
zle -N fzf-cd-widget
# change directory with ctrl+f
bindkey -M viins ^f fzf-cd-widget
# open tmux sessionizer with ctrl+t
bindkey -M viins -s ^t "tmux-sessionizer\n"
# open vscode with alt+f
bindkey -M viins -s "^[f" "vscode-sessionizer\n"

{{ if eq .chezmoi.os "linux" }}
# export PYENV_ROOT="$HOME/.pyenv"
# [[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"
# eval "$(pyenv init -)"

# pnpm
# export PNPM_HOME="/home/pszedmak/.local/share/pnpm"
# case ":$PATH:" in
#   *":$PNPM_HOME:"*) ;;
#   *) export PATH="$PNPM_HOME:$PATH" ;;
# esac
# # pnpm end
# export PATH=$PATH:$HOME/go/bin
{{ end }}

# Generated for envman. Do not edit.
[ -s "$HOME/.config/envman/load.sh" ] && source "$HOME/.config/envman/load.sh"

{{ if eq .chezmoi.os "linux" }}
# fnm
FNM_PATH="/home/thazulk/.local/share/fnm"
if [ -d "$FNM_PATH" ]; then
  export PATH="/home/thazulk/.local/share/fnm:$PATH"
  eval "`fnm env`"
fi

### MANAGED BY RANCHER DESKTOP START (DO NOT EDIT)
export PATH="/home/thazulk/.rd/bin:$PATH"
### MANAGED BY RANCHER DESKTOP END (DO NOT EDIT)

# Start ssh-agent on login
# eval "$(ssh-agent -s)"
# ssh-add ~/.ssh/id_rsa 2>/dev/null
{{ end }}

#Tmux

alias tks="tmux kill-server"

{{ if eq .chezmoi.os "linux" }}
if command -v kubectl >/dev/null 2>&1; then
  source <(kubectl completion zsh)
  compdef __start_kubectl k
fi
{{ end }}

if command -v mise >/dev/null 2>&1; then
  eval "$(mise activate zsh)"
fi

# Keep the native completion menu out of the way and expose descriptions as
# groups in fzf-tab.
zstyle ':completion:*:descriptions' format '[%d]'
zstyle ':completion:*' menu no

# fzf-tab must be the last plugin that binds Tab.
if command -v brew >/dev/null 2>&1; then
  _fzf_tab_root="$(brew --prefix fzf-tab 2>/dev/null)"
  if [[ -r "$_fzf_tab_root/share/fzf-tab/fzf-tab.zsh" ]]; then
    source "$_fzf_tab_root/share/fzf-tab/fzf-tab.zsh"
  fi
  unset _fzf_tab_root
fi
