{{ template "kiro-cli-pre.tmpl" dict "rcfile" "bashrc" "ext" "bash" }}

# ~/.bashrc

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '

# Generated for envman. Do not edit.
[ -s "$HOME/.config/envman/load.sh" ] && source "$HOME/.config/envman/load.sh"

# mise: version-managed tools (go, node, python, …)
if command -v mise >/dev/null 2>&1; then
  eval "$(mise activate bash)"
  if command -v go >/dev/null 2>&1; then
    export GOPATH="${GOPATH:-$(go env GOPATH 2>/dev/null || echo "$HOME/go")}"
    [[ -d "$GOPATH/bin" ]] && export PATH="$GOPATH/bin:$PATH"
  fi
fi

{{ template "kiro-cli-post.tmpl" dict "rcfile" "bashrc" "ext" "bash" }}
